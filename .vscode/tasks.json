{
  "version": "2.0.0",
  "tasks": [
    {
      "type": "shell",
      "label": "Spring Boot (Testes)",
      "command": "mvn clean verify",
      "options": {
        "cwd": "${workspaceFolder}/backend"
      },
      "group": "build",
      "problemMatcher": ["$msCompile"]
    }
  ]
}
